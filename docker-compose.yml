version: "3.5"
services:
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
    - "3000:3000"
    volumes:
    - "./frontend:/usr/src/app"
    - "/usr/src/app/node_modules"
